Image used rapidly starting developing smart contracts for ethereum.
Given docker is installed, just run:

```
> docker run -it --rm italomaia/truffle
```

**docker hub:** https://hub.docker.com/r/italomaia/truffle/